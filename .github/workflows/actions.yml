on: 
  push: 
    branches: master

name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2

    - name: Use Node.js 14
      uses: actions/setup-node@v2
      with:
        node-version: '14'
      
    - name: 🔨 Build Project
      run: |
        npm install
        npm run build
      env:
        CI: false
    
    - name: 📂 Sync filess
      uses: SamKirkland/FTP-Deploy-Action@4.3.3
      with:
        server: ftp.dafabaliutama.com
        username: ftpgithub@dafabaliutama.com
        password: ${{ secrets.FTP_PASSWORD }}
        dangerous-clean-state: true
        local-dir: ./build/